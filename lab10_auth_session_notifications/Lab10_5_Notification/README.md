# Lab10_5_Notification

Features:

- Uses `flutter_local_notifications`.
- Requests Android 13+ notification permission.
- Shows a local notification manually.
- Same notification method can be called after login in `Lab10_Full`.

Run on Android emulator/device:

```powershell
flutter pub get
flutter run
```
