# Lab10_3_AutoLogin_Logout

Features:

- Real DummyJSON login.
- Saves `accessToken` and username with `shared_preferences`.
- SplashScreen decides Login vs Home.
- Logout clears session and returns to Login.

Test account:

- Username: `emilys`
- Password: `emilyspass`

Run:

```powershell
flutter pub get
flutter run
```
