# Lab10_Full

Integrated features:

- SplashScreen routing.
- Real API login with DummyJSON.
- Session persistence with `shared_preferences`.
- Auto-login after restart.
- Logout clears session.
- Firebase Google Sign-In hook.
- Local notification after successful login.

Test account:

- Username: `emilys`
- Password: `emilyspass`

Firebase note:

Google Sign-In code is included. Replace demo Firebase options and configure Firebase/SHA keys before using real Google Sign-In.

Run:

```powershell
flutter pub get
flutter run
```
